apiVersion: v1
kind: Service
metadata:
  name: api-service
spec:
  type: NodePort
  selector:
    app: my-app # Esse label deve corresponder ao label do pod que fornece a API
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
